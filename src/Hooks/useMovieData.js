import React, { useEffect } from "react";
import { MOVIE_DATA, OPTIONS } from "../Utils/Contants";

export default function useMovieData(id) {
  useEffect(() => {}, []);
  return <div>useMovieData</div>;
}
